info:
  name: OpenAPI Schema
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{simulator_port}}/openapi.json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
