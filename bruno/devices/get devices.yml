info:
  name: get devices
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{simulator_port}}/api/devices/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
