info:
  name: Sensor Discovery
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:{{simulator_port}}/api/devices/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
