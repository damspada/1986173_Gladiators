info:
  name: Inject Sensor Event
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/admin/sensors/sensor-01/events
  body:
    type: json
    data: |-
      {
        "event_type": "nuclear_like"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
