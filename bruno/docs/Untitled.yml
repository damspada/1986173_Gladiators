info:
  name: Swagger Docs
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{simulator_port}}/docs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
