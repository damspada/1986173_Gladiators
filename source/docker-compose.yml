include:
  - db/docker-compose.yml
  - backend/docker-compose.yml
  - broker/docker-compose.yml
  - replicas/docker-compose.yml

services:
  simulator:
    image: seismic-signal-simulator:multiarch_v1
    ports:
      - "9090:8080"
    environment:
      SAMPLING_RATE_HZ: 100
      AUTO_SHUTDOWN_ENABLED: "true"
      AUTO_SHUTDOWN_MIN_SECONDS: 30
      AUTO_SHUTDOWN_MAX_SECONDS: 90
    networks:
      - gladiators-net

networks:
  gladiators-net:

# services:
#   simulator:
#     image: seismic-signal-simulator:multiarch_v1
#     container_name: seismic-simulator
#     ports:
#       - "${SIMULATOR_PORT:-8080}:8080"
#     environment:
#       SAMPLING_RATE_HZ: ${SAMPLING_RATE_HZ:-20}
#       AUTO_SHUTDOWN_ENABLED: ${AUTO_SHUTDOWN_ENABLED:-true}
#       AUTO_SHUTDOWN_MIN_SECONDS: ${AUTO_SHUTDOWN_MIN_SECONDS:-30}
#       AUTO_SHUTDOWN_MAX_SECONDS: ${AUTO_SHUTDOWN_MAX_SECONDS:-90}
#     networks:
#       - seismic-net

#   backend:
#     image: ${BACKEND_IMAGE:-your-team/backend:latest}
#     container_name: seismic-backend
#     ports:
#       - "${BACKEND_PORT:-8090}:8080"
#     environment:
#       SIMULATOR_BASE_URL: ${SIMULATOR_BASE_URL:-http://simulator:8080}
#     depends_on:
#       - simulator
#     networks:
#       - seismic-net

#   frontend:
#     build:
#       context: ./frontend-dashboard
#       dockerfile: Dockerfile
#       args:
#         VITE_LIVE_WS_URL: ${VITE_LIVE_WS_URL:-/api/events/ws}
#         VITE_HISTORY_API_URL: ${VITE_HISTORY_API_URL:-/api/history/events}
#     container_name: seismic-frontend
#     ports:
#       - "${FRONTEND_PORT:-5173}:80"
#     environment:
#       BACKEND_UPSTREAM: ${BACKEND_UPSTREAM:-backend:8080}
#     depends_on:
#       - backend
#     networks:
#       - seismic-net

# networks:
#   seismic-net:
#     driver: bridge