info:
  name: Broker Replica Status
  type: websocket
  seq: 1

websocket:
  url: localhost:{{broker_port}}/backend/ws
  message:
    type: json
    data: "{}"
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
