info:
  name: Get Events by Classification
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/history/events?type=CONVENTIONAL_EXPLOSION
  params:
    - name: type
      value: CONVENTIONAL_EXPLOSION
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
