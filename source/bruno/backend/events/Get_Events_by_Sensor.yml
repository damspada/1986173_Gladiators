info:
  name: Get Events by Sensor
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/history/events?sensor_id=sensor-11
  params:
    - name: sensor_id
      value: sensor-11
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
