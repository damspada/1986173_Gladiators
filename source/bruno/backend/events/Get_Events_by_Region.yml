info:
  name: Get Events by Region
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/history/events?region=Borealis
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
