info:
  name: WebSocket Live Stream
  type: http
  seq: 9

http:
  method: GET
  url: ws://localhost:{{backend_port}}/api/events/ws
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
