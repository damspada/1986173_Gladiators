info:
  name: Get Corroboration
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/history/events/corroboration
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
