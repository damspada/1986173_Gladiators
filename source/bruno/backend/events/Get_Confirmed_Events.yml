info:
  name: Get Confirmed Events
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/history/events/confirmed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
