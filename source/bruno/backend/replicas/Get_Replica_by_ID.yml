info:
  name: Get Replica by ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/replicas/d0d7fefeedcf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
