info:
  name: Get Connected Replicas
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/replicas/connected
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
