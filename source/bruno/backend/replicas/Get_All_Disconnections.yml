info:
  name: Get All Disconnections
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/replicas/disconnections
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
