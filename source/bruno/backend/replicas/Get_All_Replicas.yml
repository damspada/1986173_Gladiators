info:
  name: Get All Replicas
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:{{backend_port}}/api/replicas
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
