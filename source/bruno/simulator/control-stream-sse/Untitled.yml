info:
  name: Control Stream (SSE)
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:{{simulator_port}}/api/control
  headers:
    - name: Accept
      value: text/event-stream
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
