info:
  name: Control Stream (SSE)
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/control
  auth: inherit
  headers:
    - name: Accept
      value: text/event-stream
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
